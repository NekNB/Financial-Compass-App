<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name="ru.financecompas.app.activities.quiz.QuizActivity"
            android:exported="false"
            android:label="@string/title_activity_quiz"
            android:theme="@style/Theme.MyApplication" />
        <activity android:name="ru.financecompas.app.activities.reallife.RealLife"
            android:exported="false"
            android:label="@string/title_activity_real_life"
            android:theme="@style/Theme.MyApplication" />
        <activity android:name="ru.financecompas.app.activities.thirdGame.ThirdGame"
            android:exported="false"
            android:label="ThirdGame"
            android:theme="@style/Theme.MyApplication" />
        <activity android:name="ru.financecompas.app.activities.fourthGame.FourthGame"
            android:exported="false"
            android:label="FourthGame"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name="ru.financecompas.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>